{
  "name": "chalk",
  "scripts": {
    "setup": "bin/setup",
    "local": "bundle exec jekyll serve --drafts",
    "dev": "bundle exec jekyll serve --drafts --incremental --livereload",
    "fast": "bundle exec jekyll serve --drafts --incremental --limit_posts 1",
    "publish": "bin/deploy"
  },
  "dependencies": {
    "@mdi/font": "^7.4.47",
    "jquery": "^3.2.1",
    "npm": "^6.0.1",
    "retinajs": "^2.1.1",
    "svgxuse": "^1.2.4",
    "webfontloader": "^1.6.28",
    "zooming": "^2.0.0"
  }
}
